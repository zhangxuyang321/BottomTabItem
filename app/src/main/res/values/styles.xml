<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="bottomitem">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_gravity">center_vertical</item>
        <item name="android:layout_weight">1</item>
        <item name="custom:iconHeight">20dp</item>
        <item name="custom:iconWidth">20dp</item>
        <item name="custom:titleSelectColor">"#50c1ea</item>
        <item name="custom:titleSize">12dp</item>
        <item name="custom:titleTop">2dp</item>
    </style>

</resources>